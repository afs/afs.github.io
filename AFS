Pure static site:

Empty file ".nojekyll"

then HTML in /docs?


-------------------

"jekyll build"

"jekyll server" -> localhost:4000

Keep Jekyll up-to-date:
"bundle update"

Jekyll 2:
  For jekyll-coffeescript (required)
sudo gem install execjs

sudo gem install therubyracer

or instead of therubyracer
  sudo apt-get install nodejs


    <li>
      <a href="{{ post.url }}">{{ post.title }}</a>
      {{ post.excerpt }}
    </li>

for a excerpt in the Blog page.
But it goes wild on some imported blogger pages.

* Import blogger

ruby -rubygems -e 'require "jekyll-import";
    JekyllImport::Importers::Blogger.run({
      "source"                => "/home/afs/Desktop/blog-10-10-2014.xml",
      "no-blogger-info"       => false, # not to leave blogger-URL info (id and old URL) in the front matter
      "replace-internal-link" => false, # replace internal links using the post_url liquid tag.
    })'

CNAME file:
One domain only

<link href='http://fonts.googleapis.com/css?family=Source+Sans+Pro:400,700' rel='stylesheet' type='text/css'>
16px? 17px?

*** If the *.md file is in _site, not the .md, then check the Jekyll md header.
